* XHR Polling transport
* upgrade transport
* CORS support for XHR Polling
* multi-packets messages (?)
* use Guava instead of util.IO
* use com.fasterxml.jackson instead of util.JSON
* handle b64 flag
* make Session thread-safe (There are other classes that is not thread-safe)
