* multi-packets messages (Payload for EIO. It seems to be relevant only for polling transport)
* XHR Polling transport
* upgrade transport
* CORS support for XHR Polling
* handle b64 flag
* make Session thread-safe (There are other classes that is not thread-safe)
